--- Terminal settings
return {
    {
        "akinsho/toggleterm.nvim",
        version = "*",
        opts = {
            open_mapping = [[<m-,>]],
            -- open_mapping = [[<C-n>]],
        },
    },
}
